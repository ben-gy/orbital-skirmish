{
  "name": "orbital-skirmish",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Dogfight in a shared gravity well — thrust, slingshot around the star, and be the last ship flying.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "trystero": "^0.21.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0",
    "jsdom": "^25.0.0"
  }
}
